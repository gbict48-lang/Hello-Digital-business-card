import Foundation

/// Fixed identifiers for the Wallet pass. These are baked in so the app "just
/// works" — there is nothing for the user to configure.
enum PassConstants {
    static let teamIdentifier = "476FB5QW34"
    static let passTypeIdentifier = "pass.nl.gbict.hellodigitalbusinesscard"
    static let organizationName = "Hello Digital Business Card"
}
